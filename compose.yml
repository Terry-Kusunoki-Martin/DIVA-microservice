services:
  append-text-naive:
    image: valleyforge/append-text-naive:latest
    build: .
    volumes:
      # Uncomment below as needed
      # - data:/data
      # - resources:/resources
    healthcheck:
      test:
        ["CMD-SHELL", "curl -fsS localhost:8080/health | jq .status | grep UP"]

# Uncomment below as needed

# volumes:
#   data:
#   resources:
